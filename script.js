for (let index = 1; index <= 25 ; index++) {
    if(index < 10 ){
        console.log(`0${index},`)
    }else if (index ===25) {
     console.log(`${index}`)
    }else {
        console.log(`${index},`)
    }

    
}